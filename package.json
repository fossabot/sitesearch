{
  "name": "root",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "bun --filter docs dev",
    "build:docs": "bun --filter docs build",
    "bundle": "bun --filter @algolia/sitesearch run build",
    "bundle:zip": "bun --filter @algolia/sitesearch build && cd packages/standalone/dist && zip -r sitesearch-bundles.zip search*.{min.js,min.css} search-askai*.{min.js,min.css}",
    "dev:vanilla": "bun --filter @algolia/sitesearch dev & bun --filter vanilla serve"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "bun@1.1.29",
  "devDependencies": {
    "@biomejs/biome": "2.0.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "bun": ">=1.0.0"
  }
}
